Classroom Info
<%= render 'classroom', locals: { classroom: @classroom} %>

We now want to call out the oldest student in the class:
<% @classrom.students.each do |student| %>
<%= render 'students/student', locals: { student: student } %>
<% end %> 
